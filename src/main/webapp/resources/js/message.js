function sussesMessageBottom(message) {
    noty({
        text: message,
        type: 'success',
        layout: 'bottomRight',
        timeout: 3000
    });
}
function errorMessageBottom(message) {
    noty({
        text: message,
        type: 'error',
        layout: 'bottomRight',
        timeout: 3000
    });
}
function sussesMessageCenter(message) {
    noty({
        text: message,
        type: 'success',
        layout: 'bottomRight',
        timeout: 3000
    });
}
function errorMessageCenter(message) {
    noty({
        text: message,
        type: 'error',
        layout: 'bottomRight',
        timeout: 3000
    });
}
